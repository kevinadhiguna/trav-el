// Use 'querySelectorAll' to make all pictures able to displayed bigger. Otherwise, only the first picture will work.
const mb = document.querySelectorAll('.materialboxed');

M.Materialbox.init(mb, {});
